@inherits BsControlBase<string>
    <BsGroup Label="@Label" TType="string">
        <input type="text" readonly tablindex="-1" class="form-control-plaintext" id="@Id" value="@CurrentValue">
    </BsGroup>
@code
{
    protected override bool TryParseValueFromString(string value, out string result, out string validationErrorMessage)
    {
        validationErrorMessage = "Should not be updating values";
        result = null;
        return false;
    }

}